<div class="page-header" style="background-image: url(<%= asset_path('welding.jpg') %>);" >
	<div class="container">
		<div class="row">
			<h1>Projects</h1>
		</div>
	</div>
</div>

<div class="container">
	<%= @projects.each do |project| %>
		<div class="row">
			<h1><%= project.name %></h1>
			<p><%= project.description %></p>
		</div>
	<% end %>
</div>